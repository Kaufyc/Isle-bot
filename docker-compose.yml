services:
  isle-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: isle-evrima-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: ["python", "src/main.py"]
